# Este archivo define los canales de entrada por los que el asistente
# puede recibir mensajes.

# Activa el canal para la API REST.
rest:

# Activa el canal de Socket.IO, necesario para el chat web.
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true